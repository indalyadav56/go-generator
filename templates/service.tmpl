{{define "service"}}
package services

import "{{.ServiceName}}/internal/{{.AppName}}/repository"

type {{.IAppName}}Service interface {
	// Create(user *models.User) (*models.User, error)
	// GetByEmail(email string) (*models.User, error)
	// GetByID(id string) (*models.User, error)
	// List() ([]models.User, error)
}

type {{.AppName}}Service struct {
	{{.AppName}}Repo repository.{{.IAppName}}Repository
}

func New{{.IAppName}}Service(repo repository.{{.IAppName}}Repository) *{{.AppName}}Service {
	return &{{.AppName}}Service{
		{{.AppName}}Repo: repo,
	}
}

{{end}}
